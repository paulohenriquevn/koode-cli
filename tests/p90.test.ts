import {describe, it, expect} from 'vitest';

// TODO: Rewrite test suite to actually use describe/it/expect.
describe('koode', () => {
	it('runs through koode.test.ts', () => {
		expect(true).toBe(true);
	});
});
